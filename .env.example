# Required only for live extraction (npm run cache:sample, /api/extract on uploads).
# The deployed sample-PDF demo works without this — it serves public/sample.schema.json.
ANTHROPIC_API_KEY=sk-ant-...
